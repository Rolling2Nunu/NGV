# NGV

빈 상태로 시작하는 프로젝트입니다. 아직 소스 코드는 없으며, 향후 이 프로젝트가 무엇을 하는지, 사용하는 기술 스택과 아키텍처는 코드가 추가되는 대로 여기에 문서화합니다.

## 소프트웨어 개발 정보

이 소프트웨어 개발은 다음의 항목을 이용해서 개발

- 코드개발: Python 3.14
- 코드테스트: Unittest
- 순환복잡도, 함수라인수 등 측정 지표는 오픈소스 도구 이용

## 프로젝트 개발 정책

### 개발 생명 주기

- 이 프로젝트는 **반드시** 분석, 설계, 구현, 테스트의 순서대로 개발을 진행한다.
- 각 단계가 완료가 되었을 때, 지정된 템플릿을 이용한 산출물이 생성되어야 한다.

### 분석 지침

- 요구사항 분석 단계 수행은 requirements-analyzer 서브에이전트가 담당한다.

### 아키텍처 설계 지침

- 아키텍처 설계 단계 수행은 architecture-designer 서브에이전트가 담당한다.

### 구현 지침

- 구현 단계 수행은 Coding 서브에이전트가 담당한다.
- TDD 방식 적용, TDD 스킬 사용
- 아래 품질 지표 **반드시** 준수
    - 함수 라인수: 순수코드라인 50라인 이하
    - 함수 순환복잡도: 10이하
    - 중복 코드: 7라인 이하 허용
    - 주석: Doxygen 방식 적용, 20% 이상 작성 필수
- 함수명, 변수명은 3글자 이상 사용, 낙타 표기법 활용

### Git 브랜치 전략 (obra/superpowers 참고)

출처: https://github.com/obra/superpowers `using-git-worktrees`,
`finishing-a-development-branch` 스킬 반영(GitHub Flow 기반, 트렌디한
단명 브랜치 전략).

- **트렁크**: `main`. 항상 병합 가능·정상 상태 유지.
- **작업 브랜치**: 기능/작업 단위별 단명 브랜치(`feature/<slug>`),
  장기 존속 금지 — 완료되면 즉시 병합 후 삭제.
- **격리 작업공간**: 브랜치 작업 시 가능하면 `git worktree`로
  `.worktrees/<branch>`에 격리(현재 체크아웃 오염 방지).
  `.worktrees/`는 `.gitignore` 처리, 커밋 금지.
- **완료 절차**: (1) 테스트 전체 통과 확인 → (2) 통합 방식 선택
  (로컬 병합 / Push 후 PR 생성 / 보류) → (3) 병합 시 결과에서도
  테스트 재확인 → (4) worktree·브랜치 정리.
- **공유 반영**: `origin`으로의 병합은 원칙적으로 PR을 통해서(직접
  main push 지양). "커밋"은 로컬 전용, "push"·"PR 생성"·"병합"은
  사용자가 명시적으로 요청할 때만 수행(기존 커밋/푸시 정책 유지,
  범위만 PR 단계까지 확장).
- 작업 폐기는 사용자가 "폐기"라고 명시할 때만 수행, 임의 강제 삭제 금지.

### 문서 작성 스타일

- Claude의 대화 응답과 이 프로젝트의 모든 문서(요구사항 명세서, 아키텍처
  설계서, 감사 보고서 등 기술 산출물 포함)는 단답형(개조식: 명사형 종결,
  불필요한 서술어/수식어 생략)으로 작성한다.
- 예: "투 파일을 생성", "그에 맞게 갱신", "커밋/푸시 필요시 요청 필수".
- 필수 정보(ID, 근거, 수치, 절차 등)는 생략하지 않되, 문장은 최대한 짧게 쓴다.
